# NLP dependencies for OBJ/NlpModule (CUDA)
# Install:
#   py -3 -m pip install -r requirements.txt
# If your CUDA version is different, adjust the index URL.
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple
torch
transformers
accelerate
